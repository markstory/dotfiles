[color]
	branch  = auto
	diff = auto
	status = auto
[alias]
	br = branch
	co = checkout
	df = diff
	st = status -sb -uno
	dc = diff --cached
	gl = log --pretty=short --decorate=short --graph
	lc = log ORIG_HEAD.. --stat --no-merges
	ca = commit --amend
[color "status"]
	added = yellow
	changed = green
[color "diff"]
	meta = cyan bold
	frag = magenta bold
	old = red bold
	new = green bold
[user]
	name = Mark Story
	email = mark@freshbooks.com
[push]
	default = current
