[color]
	branch  = auto
	diff = auto
	status = auto
[alias]
	br = branch
	co = checkout
	ci = commit
	df = diff
	st = status -sb -uno
	dc = diff --cached
	gl = log --pretty=short --decorate=short --graph
	lc = log ORIG_HEAD.. --stat --no-merges
	ca = commit --amend
	nb = checkout -b
	cp = cherry-pick
[color "status"]
	added = yellow
	changed = green
[color "diff"]
	meta = cyan bold
	frag = magenta bold
	old = red bold
	new = green bold
[user]
	name = Mark Story
	email = mark@mark-story.com
[github]
	user = markstory
[push]
	default = current
